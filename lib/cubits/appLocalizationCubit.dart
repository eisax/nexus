import 'package:flutter/material.dart';

class AppLocalizationState extends StatelessWidget {
  const AppLocalizationState({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}